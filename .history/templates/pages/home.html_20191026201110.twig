{% extends 'base.html.twig' %}

{% block title 'Agence DI' %}

{% block body %}
    <div class="jumbotron text-center">
    <h1>Bienvenue chez Decoopman Immobilier</h1>
    <p.text-primary>Lorem ipsum dolor sit amet consectetur adipisicing elit. Sed ipsam repellat nam labore dolore ad natus explicabo vero velit ea deleniti eveniet ipsa, architecto nihil.</p>

    </div>
    <div class="container">
        <h2>Les derniers biens</h2>
        <div class="row flex ">
        {% for property in properties %}
            <div class="col-3">
                <div class="card mt-4">
                    <div class="card-body">
                        <h5 class="card-title">
                        <a href="{{ path('property.show', {id: property.id, slug: property.slug}) }}">{{ property.title }}</a>
                        </h5>
                        <p class="card-text">{{ property.city }} ({{ property.postalCode }})</p>
                        <div class="text-primary" style="font-weight: bold;font-size: 2rem;">{{ property.formattedPrice }} €</div>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}